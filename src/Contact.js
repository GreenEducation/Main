const contactStyles = {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center'
};

function Contact(props) {
    return(<div style={contactStyles}>Contact</div>);
}

export default Contact;