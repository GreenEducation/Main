const aboutStyles = {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center'
};

function About(props) {
    return(<div style={aboutStyles}>About</div>);
}

export default About;